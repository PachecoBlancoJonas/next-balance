# MariaDB root credentials (used only by create_db.sh)
DB_ROOT_USER=root
DB_ROOT_PASSWORD=

# App database config (must match backend/.env)
DB_HOST=localhost
DB_USER=next_balance
DB_PASSWORD=your_db_password
DB_NAME=next_balance
